<odoo>
    <template id="portal_my_employees_template" name="My Employees">
       <t t-call="website.layout">
        <div class="container mt16">
            <h3>Employees List</h3>

            <a href="/create_employee_form" class="btn btn-primary mb16">Create New Employee</a>

            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Employee Name</th>
                        <th>Manager</th>
                    </tr>
                </thead>
                <tbody>
                    <t t-foreach="employees" t-as="emp">
                        <tr>
                            <td><t t-esc="emp.name"/></td>
                            <td>
                                <t t-if="emp.parent_id">
                                    <t t-esc="emp.parent_id.name"/>
                                </t>
                                <t t-else="">No Manager</t>
                            </td>
                        </tr>
                    </t>
                </tbody>
            </table>
        </div>
    </t>
    </template>
</odoo>
